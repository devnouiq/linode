apiVersion: v1
kind: Namespace
metadata:
  name: tenant-2d117
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tenant-2d117-premium
  namespace: flux-system
spec:
  releaseName: tenant-2d117-premium
  targetNamespace: tenant-2d117
  storageNamespace: tenant-2d117
  interval: 1m0s
  chart:
    spec:
      chart: helm-tenant-chart
      version: "1.9.x"
      sourceRef:
        kind: HelmRepository
        name: helm-tenant-chart
  values:
    tenantId: tenant-2d117
    apps:
      producer:
        enabled: true # Silo deployment -- premium tier has a dedicated deployment for each tenant
        ingress:
          enabled: true
        image:
          tag: "0.1" # {"$imagepolicy": "flux-system:producer-image-policy:tag"}
      consumer:
        enabled: true # Silo deployment -- premium tier has a dedicated deployment for each tenant
        ingress:
          enabled: true
        image:
          tag: "0.1" # {"$imagepolicy": "flux-system:consumer-image-policy:tag"}
