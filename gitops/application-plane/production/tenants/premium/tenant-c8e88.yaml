apiVersion: v1
kind: Namespace
metadata:
  name: tenant-c8e88
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tenant-c8e88-premium
  namespace: flux-system
spec:
  releaseName: tenant-c8e88-premium
  targetNamespace: tenant-c8e88
  storageNamespace: tenant-c8e88
  interval: 1m0s
  chart:
    spec:
      chart: ./helm-charts/helm-tenant-chart
      version: "1.9.x"
      sourceRef:
        kind: GitRepository
        name: helm-tenant-chart
  values:
    tenantId: tenant-c8e88
    apps:
      producer:
        enabled: true # Silo deployment -- premium tier has a dedicated deployment for each tenant
        ingress:
          enabled: true
        image:
          tag: "0.1" # {"$imagepolicy": "flux-system:producer-image-policy:tag"}
      consumer:
        enabled: true # Silo deployment -- premium tier has a dedicated deployment for each tenant
        ingress:
          enabled: true
        image:
          tag: "0.1" # {"$imagepolicy": "flux-system:consumer-image-policy:tag"}
