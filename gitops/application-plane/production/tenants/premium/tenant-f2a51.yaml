apiVersion: v1
kind: Namespace
metadata:
  name: tenant-f2a51
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tenant-f2a51-premium
  namespace: flux-system
spec:
  releaseName: tenant-f2a51-premium
  targetNamespace: tenant-f2a51
  storageNamespace: tenant-f2a51
  interval: 1m0s
  chart:
    spec:
      chart: helm-tenant-chart
      version: "1.9.x"
      sourceRef:
        kind: HelmRepository
        name: helm-tenant-chart
  values:
    tenantId: tenant-f2a51
    apps:
      producer:
        enabled: true # Silo deployment -- premium tier has a dedicated deployment for each tenant
        ingress:
          enabled: true
        image:
          tag: "0.1" # {"$imagepolicy": "flux-system:producer-image-policy:tag"}
      consumer:
        enabled: true # Silo deployment -- premium tier has a dedicated deployment for each tenant
        ingress:
          enabled: true
        image:
          tag: "0.1" # {"$imagepolicy": "flux-system:consumer-image-policy:tag"}
